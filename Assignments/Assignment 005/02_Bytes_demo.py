# Predict the output:
b = bytes([65, 66, 67])
print(b)
# Explain how the numbers are converted internally

# Solution:
# Output:
# b'ABC'

# Explaination: Bytes is binary data type. The list of numbers provided will be converted to their ASCI code in binary data.